# Workspace Rules

- Whenever the user asks to "push", "push code", or "push to github", automatically stage all changes (`git add .`), commit with a descriptive commit message based on the recent changes, and push to `origin main`.
- The git remote `origin` is already configured with authentication credentials.
